#ifndef NUMS_HPP
#define NUMS_HPP

#include <algorithm>
#include "helper_functions.hpp"

enum NumsArguments {
	NUM_VALUES = 0,
	MAX_VALUE = 1,
	MIN_VALUE = 2,
	GROUPING = 3,
	SORTING = 4,
};

void generateNumbers();

#endif
